{
  "name": "nanoasar",
  "version": "4.0.3",
  "type": "module",
  "main": "asar.mjs",
  "module": "asar.mjs",
  "types": "types/asar.d.ts",
  "files": [
    "asar.mjs",
    "types/"
  ],
  "scripts": {
    "prepublish": "bun build.ts && bun test",
    "build": "bun build.ts",
    "test": "bun build.ts && bun test"
  },
  "devDependencies": {
    "@electron/asar": "4.0.1",
    "esbuild": "0.27.3"
  }
}
